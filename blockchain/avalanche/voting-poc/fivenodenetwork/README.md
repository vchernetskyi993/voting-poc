# Docker image for Avalanche local network

Initial try to pack working local Avalanche network into Docker image.

Current issue: Nodes aren't able to check health of each other inside the container.
