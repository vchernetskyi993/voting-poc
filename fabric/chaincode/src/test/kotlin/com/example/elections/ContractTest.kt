package com.example.elections

class ContractTest {

    /*
    TODO: requirements:
     1. Create election:
        * should store election
        * only government is allowed to create
        * should check start & end dates
        * should validate candidates array
     2. Elections count
        * check no elections returns 0
     3. Fetch election
     4. Vote:
        * should vote
        * should not vote twice
        * should check start & end date
     */
}